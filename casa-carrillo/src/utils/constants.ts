/** Umbral de compra para envío gratis (COP). */
export const FREE_SHIPPING_THRESHOLD = 150_000

/** Costo de envío plano cuando no aplica gratis (COP). */
export const FLAT_SHIPPING = 12_000

export const DEFAULT_PAGE_SIZE = 9

/** Latencia simulada del mock API (ms). */
export const MOCK_LATENCY = 450
